vim.opt.list = true

require("indent_blankline").setup {
  -- disable and let the guides toggle on with |
  enabled = false,
  -- space_char_blankline = " ",
  -- show_current_context = true,
  -- show_current_context_start = true,
  -- char_list = {'|', '¦', '┆', '┊'},
  -- use_treesitter = true,
  -- context_patterns = {
  --   '^if',
  --   '^for',
  --   '^switch',
  --   '^select',
  --   '^def',
  --   '^try',
  --   '^class',
  --   '^func',
  --   '^with',
  --   '^spec',
  -- }
}
